@model IEnumerable<MaouHeroLanding.Models.Compras>

@{
    ViewBag.Title = "Index";
}

<h2>Compras</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artigo.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Encomenda.Local_entrega)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.preco)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artigo.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Encomenda.Local_entrega)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.preco)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
            @Html.ActionLink("Details", "Details", new { id = item.id }) 
            <!-- | @Html.ActionLink("Delete", "Delete", new { id=item.id }) -->
        </td>
    </tr>
}

</table>
